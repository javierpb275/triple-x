#include <iostream>

int main()
{
    std::cout << "Hello World!";
	return 0;
}

//std: namespace (prevent name conflicts in our code). we access the namespace by using the "::".
//:: : Scope Operator. we tell the compiler to check the code inside the namespace.
//cout: to print sth in the console
// << : allow us to output the text to the right